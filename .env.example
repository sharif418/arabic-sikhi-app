# Database
DATABASE_URL=file:/home/z/my-project/db/custom.db

# Session — MUST be a strong random string in production (at least 32 bytes)
# Generate with: openssl rand -base64 32
SESSION_SECRET=change-me-to-a-strong-random-secret

# AI (z-ai-web-dev-sdk) — configured automatically by the sandbox
